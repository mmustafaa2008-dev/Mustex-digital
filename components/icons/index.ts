/**
 * Icon utilities re-export.
 * Keep Lucide composition in lib/icons — no page-level icon components here yet.
 */
export {
  createIconProps,
  iconColors,
  iconHoverPresets,
  iconSizes,
  iconStates,
  iconStrokes,
  mergeIconProps,
} from "@/lib/icons";
