"use client";

/**
 * LazyMotion-compatible motion factory.
 * Import `m` instead of `motion` under LazyMotion `strict` mode.
 */
export { m } from "framer-motion";
export { AnimatePresence } from "framer-motion";
